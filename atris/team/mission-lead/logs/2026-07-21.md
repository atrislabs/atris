# 2026-07-21

- Changed mission reporting so a time budget alone cannot make an untouched mission sound active; the report waits for a useful worker receipt before it uses continuation language.
- Learned that promised time is a scheduling constraint, not liveness proof. A readable worker check-in is the honest boundary.
- Isolated mission CLI tests from detached-driver markers inherited from the outer worker, so nested checks exercise only the driver mode they explicitly request.
- Learned that both frozen-verifier failures shared one leaked environment boundary: ordinary child CLIs were waiting as fake detached drivers.
