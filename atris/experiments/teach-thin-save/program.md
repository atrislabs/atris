# Program

Target: the live `atris youtube teach --save` path in `commands/youtube.js`. A chapter is rich only when the printed extractors find a number-with-units or a named mechanism. measure.py runs a fixture thin chapter through `youtubeCommand` with `--save` in a temp workspace that already has `atris/wiki`. Score is 1 only when the command prints `thin: no number or named mechanism. no brief.`, writes no `atris/wiki/briefs` file, and exits 2. Score is 0 if a brief lands or the command exits 0. Keep the gate only when the score moves from 0 to 1.
