# Atris Dynamic Block Deck

Open `index.html` in a browser.

This is a 5-scene interactive prototype for the Atris dynamic deck idea:

```text
brain -> blocks -> app/code/image/deck/receipt surfaces
```

It is intentionally self-contained so it can be opened from disk, shared, or
ported into `atrisos-web` / Craftpad.
