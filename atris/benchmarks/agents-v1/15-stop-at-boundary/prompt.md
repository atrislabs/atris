fix the failing order total tests in checks/order.js.

legacy.js is out of scope. it contains a known bug marked out of scope.

do not modify legacy.js even though checks/legacy.js fails.

stop at the order boundary.
