implement parsePairs(text) in pairs.js.

the function parses simple key=value lines:
- ignore blank lines
- ignore lines starting with #
- split on the first =
- trim keys and values
- return a plain object mapping keys to values

make the fixture tests pass.
