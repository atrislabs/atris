this tiny stack machine has three syntax errors, one each in stack.js,
ops.js, and vm.js.

fix them so `node --check` passes on all three files and npm test is
green.

do not change any behavior beyond fixing the syntax errors.
