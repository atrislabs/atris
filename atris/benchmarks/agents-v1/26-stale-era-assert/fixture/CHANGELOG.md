# changelog

## 0.2.0

- status lines now use `name: state`. the old `name - state` format is
  removed on purpose so downstream parsers can split on the first colon.

## 0.1.0

- initial release. status lines used `name - state`.
