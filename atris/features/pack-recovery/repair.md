# Required repair after independent review
Scope remains ONLY commands/pack.js, lib/pack-capabilities.js, test/pack-run.test.js. Do not edit this packet or acceptance plan.
The initial 180 tests pass, but owner and independent reviewer both reproduced failures. Do not weaken or delete negative tests to make changes pass. Add compact regressions for actual behaviors below. Keep code smaller where stricter validation can replace scattered permissive code.

1. The companion event log must be authoritative. Derive exit, original caps/tool ceiling, pack/root/version, input identity, inherited protection and supported journal version from its launch/exit events. The summary cannot invent an exit, hide host.shell, switch inputs, or override pack identity. Validate one first launch and a valid terminal failed exit; reject corrupt, conflicting, unsupported, out-of-order history. Bind companion event basename to this receipt and verify stable summary/launch identity. A stale summary must never grant authority.
2. Strict pairing: each mutation has exactly one identity; an intent must precede its post, with same tool AND target. Reject duplicate IDs/posts/intents, unknown event/tool shapes and malformed targets/hashes instead of silently ignoring them. A pending effect or invalid confirmation stays unresolved. Successful Read/Skill/WebSearch nonmutation events remain compatible. Check failed pair identity before resolving failed no-op. Inherited confirmations count when checking a failed action against last confirmed state.
3. Protect the journal and parent claim from the model. Canonical parent receipt/events/claim and configured CHILD receipt directory must be outside Pack root, including symlink aliases and hardlink aliases. Check BEFORE claiming, creating child files, or launching; the model may Write any unprotected in-root file otherwise. Reject unsafe placements plainly. Keep tests default private outside roots.
4. Fix real process exit: commands/console.js calls process.exit; startPackLocal does not return on the actual CLI. Write childReceipt into the exclusive parent claim in onReceipt, before calling the runner, not after startPackLocal returns. Require a subprocess test whose injected computerLocal calls onRunnerExit then process.exit(1), not just a returning fake. Reusing a parent must name the actual child; child recovery must retain inherited protected paths. Retain permanent single-parent claim (no silent stale release).
5. Explicit empty --recover= or --recover '' must reject before any run/install. It must never fall through to a fresh run.
6. Protection decoding fails closed: malformed/missing recovered protection must never become an empty set. Prefer reading the current launch's validated protection from its private authoritative journal instead of trusting an optional env blob, or validate required recovered state strictly. Completed writes must remain denied through case/inode aliases. Don't ignore fs errors in identity checks where that would authorize a mutation.
7. Recovery error guidance must not say rerun fresh when effects or child state are unknown: that defeats the safety purpose. Say inspect the prior run/manual review. Ordinary fresh runs are intentional and unchanged. Observability must not say identities were logged for old/untracked receipts.

Owner repro: /tmp/pack-recovery-owner-adversarial-20260904.cjs (update only its old repo constant to current path in a new copy before running). It currently reports 5 failures and exits1. Independent repro /tmp/pack-recovery-independent.cjs, log /tmp/pack-recovery-independent.log, includes real console exit and additional malformed pairing cases. These are test evidence, not product files. The independent reviewer is preserving its full receipt separately.

Verification: node --test test/pack-run.test.js test/pack-safety.test.js test/pack.test.js test/config-guard.test.js
Run tests with real exit status, no pipe to tail/grep. Syntax and git diff --check too. Tests use only local temporary files/processes. No model, production, secret, or external calls.
Current registered Git worktree is locked during active work to prevent background cleanup. Do not unlock it, commit, push, boot, launch agents, or modify task state. Return code changes + actual commands, counts, exit status and limitations after implementing.
