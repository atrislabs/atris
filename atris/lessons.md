# lessons.md — What We Learned

> Append-only. One line per lesson. Harvested by validator after every feature.

---

- **[2026-04-13] no-verify-field** — fail — [resolved] Task "Break down inbox idea: "demo"" has no explicit **Verify:** field in TODO.md. Tick halted — now only endgame tasks require verify, reactive tasks use npm test as default (fixed in v3.3.0).

- **[2026-04-09] reward-checksum-layered-guards** — pass — Judge immutability enforced at two layers: git pre-commit hook blocks staging, runtime test + judge integrity guard catch bypass. Hook alone isn't enough (—no-verify can bypass it); test + runtime check are the failsafe. For critical constants, combine git-level + runtime verification.

- **[2026-02-09] validate-md-gap** — pass — The validate.md template existed but nothing in the system told agents to use it. Three places needed updating: README, CLI output, workflow docs. Always check that templates are wired into the actual workflow, not just sitting in _templates/.
- **[2026-02-09] wire-the-loop** — pass — Same pattern one level up: specs said the right things but CLI commands didn't pass the info through. When you add a new artifact (like lessons.md), grep every command that surfaces context files and add it there too. The wiring layer is always the gap.
- **[2026-02-10] map-line-drift** — fail — MAP.md line references drift silently when functions grow. Start lines drift by 2-4 (minor), but end lines drift by 30-200 (major) because new features get appended inside existing functions. After any feature that adds code to workflow.js, init.js, or brainstorm.js, re-verify the MAP.md end-line for that function.
- **[2026-02-10] auth-duplication** — Auth logic exists in two places: `utils/auth.js` (canonical) and `bin/atris.js:1501-1812` (inline copy). The inline copy lacks `chmod 0o600` on credential save and has no HTTP timeout. Changing auth behavior means patching both unless you refactor to a single source.
- **[2026-02-10] unguarded-token-usage** — fail — [resolved] `commands/workflow.js`, `commands/brainstorm.js`, `commands/integrations.js`, and `bin/atris.js` (agent/chat) all call `loadCredentials()` and use the raw token without `ensureValidCredentials()`. Only `log-sync.js` and `auth.js` use the full refresh guard. Expired tokens silently fail in agent mode, workflow --execute, and integrations. Fixed in v3.6.0 — all four callsites now wrap the token fetch.
- **[2026-02-10] oauth-is-code-exchange-not-redirect** — The CLI OAuth flow opens `{APP_URL}/auth/cli` in browser, user gets a one-time code (5-min expiry), pastes it in terminal, CLI exchanges via `POST /auth/cli/exchange`. No local HTTP server needed. This is simpler but requires user to copy/paste.
- **[2026-02-14] backend-first-then-skill** — When adding new CLI capabilities, check the backend first (`atrisos-backend/backend/routers/integrations/`). Gmail drafts, calendar, drive all already had full backend support — we just needed to write the SKILL.md to expose them. Don't build backend unless it's actually missing.
- **[2026-02-14] dead-code-in-auth-flow** — Backend `auth.py:635` has a CLI-specific code path (`normalized_path == "/auth/cli"`) that never fires because the frontend login route wraps `next` into `/auth/callback?next=...`. The flow still works — it just takes an extra redirect through the general exchange path. Not broken, but dead code.
- **[2026-03-09] heal-needs-strict-only** — The old `findSymbolLine` had a loose fallback (`\bsymbol\b` anywhere on a line) that could heal MAP.md refs to wrong locations — e.g., a comment mentioning the symbol instead of its definition. Strict-only patterns (function/const/class/def declarations) are safer. False negative (unhealable) beats false positive (healed to wrong line).
- **[2026-04-06] map-context-needs-identifiers** — MAP.md refs with descriptive context like "(main routing logic)" or "(prints table)" can't be auto-healed because the healer searches for identifiers. Use actual symbol names: "(knownCommands dispatch)", "(cycleTimings forEach)". The healer verifies these, descriptions it can't.
- **[2026-04-06] schema-over-infrastructure** — The Karpathy knowledge wiki pattern doesn't need new services or endpoints. Adding a `last_compiled` + `sources` frontmatter contract to atris.md makes existing agents into knowledge compilers. The schema tells the LLM what to do — no plumbing needed.
- **[2026-04-06] suggestion-engine-needs-skip-keys** — When autopilot suggests tasks in dry-run or skip mode, each suggestion kind needs a unique skip key (e.g., 'fix-map-refs', 'incomplete-features', 'review'). Without this, the same suggestion repeats every iteration.
- **[2026-04-06] features-outlive-status** — Feature idea.md files say "planning" long after the feature ships. The autopilot suggestion engine found this: brainstorm, cli-ux-simplification, and team-member-standard were all "planning" but fully implemented. Validate.md creation is the right trigger to update status.
- **[2026-04-08] benchmark-prompt-paths** — fail — Endstate dry-run receipt tests can pass while real benchmark prompts are malformed, because `runTaskOnce()` never executes in dry-run. For benchmark harness changes, add one direct prompt assertion alongside receipt tests so the live path is covered before closeout.
- **[2026-04-08] clean-absolute-source-paths** — fail — `commands/clean.js` used `path.join(cwd, source)` to resolve wiki source paths, silently mangling absolute paths into `<cwd>/<abs-path>` and reporting every absolute-path source as "missing". The wiki convention is absolute paths in frontmatter (per ingest task notes C7/C18/C22). Always guard with `path.isAbsolute(p) ? p : path.join(base, p)` when a config field accepts either form.
- **[2026-04-08] no-authed-route-group** — pass — atrisos-web has no `app/(authed)` route group; the protected surface is a flat allowlist in `middleware.ts:5-13` (dashboard/account/profile/settings/billing/payment). Task descriptions referencing `app/(authed)` are wrong — verify route group assumptions against middleware.ts before writing wiki tasks for Next.js projects.
- **[2026-04-08] inbox-trigger-survives-shipping** — pass — Endgame `wiki-for-atrisos-web` shipped end-to-end (T1–T4e, C15–C55) but inbox item I1 stayed in `## Inbox`, so autopilot kept re-spawning "Break down inbox idea" validator ticks. When the breakdown of an inbox idea is shipped, move the inbox item to `## Completed` in the same closeout — otherwise the trigger keeps firing redundant ticks.
- **[2026-04-08] inbox-parser-eats-hr-separator** — fail — [resolved] `commands/autopilot.js:116`, `commands/run.js:157`, and `commands/workflow.js:81` all filtered inbox bullets with `l.trim().startsWith('-')`, which silently treats the trailing `---` markdown horizontal-rule separator as a bullet; `.replace(/^-\s*/, '')` then strips one dash and yields `--`, so an empty inbox spawns a "Break down inbox idea: '--'" tick on every autopilot run. Fix: require `- ` (dash + space) and ≥1 trailing char in any inbox-bullet filter — never trust bare `startsWith('-')` against markdown that contains `---` separators.
- **[2026-04-08] inbox-parser-fix-shipped** — pass — [resolved] Patched `commands/autopilot.js:116` to the same `- ` + length>2 filter already used by run.js and workflow.js; `atris autopilot --dry-run` now emits zero inbox-kind suggestions, proving the loop's own validator-written lesson round-tripped into a shipped code change.
- **[2026-04-08] getContextFiles-dead-return** — fail — [resolved] `commands/autopilot.js` `getContextFiles()` has TWO `return` statements: the first returns the raw array, the second (intended bullet-list dedupe) is unreachable dead code. The raw array gets joined by `Array.toString()` (commas) when interpolated into the prompt template, breaking the documented "Read these files first:\n- file1\n- file2" contract. Two T7 tests in `experiments.test.js:237,259` already document the expected bullet format and currently fail. Verified resolved during v3.6.0 self-heal sweep — function now has one return that produces bullet format; `test/experiments.test.js:252` passes.
- **[2026-04-08] horizon-imaginer-finds-known-defects** — pass — First live call of `proposeCandidateHorizons` from a clean validator tick returned the `getContextFiles-dead-return` bug as its top-confidence (0.92) horizon, citing the exact failing tests and lessons.md entry. Confirms the helper is wired to real workspace signals (lessons + wiki STATUS + git log) and not hallucinating: when /autopilot starts self-seeding, expect the first imagined horizons to be already-known-but-deferred fixes the prior loop wrote down — that's the system catching its own backlog, not a bug.
- **[2026-04-08] healer-needs-after-context** — fail — [resolved] `commands/clean.js:198` matches the symbol from the parens AFTER a ref (`file.js:123 (symbolName)`), so MAP.md "Function Inventory" entries written `\`autopilotAtris()\` → \`commands/autopilot.js:840-1004\`` (symbol BEFORE the ref) get no symbol to verify against and are silently treated as valid even when wildly drifted. Caught while validating M4: the healer fixed 4 normal drifts but left `autopilotAtris() → :674-838` stale until hand-corrected. Fixed in v3.6.0 via pre-pass cache keyed on (file:line) from `` `sym()` → `file:line` `` patterns.
- **[2026-04-08] human-output-t21-fail** — fail — End-to-end validate of the chief-of-staff surfaces flunked all three: autopilot tick 39 lines (>20 budget), `atris status` 2 lines over and lacks an explicit decision line, `atris review` still prints a pre-run banner with a 93-char line and never emits a validator-pass block. Bonus: `atris status` default routing is hijacked by `.atris/business.json` into `businessStatus('pallet')` — the chief-of-staff rewrite only lands on workspaces without a business file.
- **[2026-04-08] status-route-un-hijack** — pass — [resolved] `bin/atris.js` used to auto-detect `.atris/business.json` and silently flip plain `atris status` into `businessStatus(slug)`, which hid the local chief-of-staff surface behind ambient cwd state. Fix: plain `atris status` now always runs local `statusAtris(...)`; remote/business status requires an explicit positional slug. Rule: when a command has a local mode and a heavier remote mode, never let cwd state pick the remote one by default.
- **[2026-04-09] verify-execution-wired** — pass — Verify execution integrated cleanly into validator phase: getVerifyCommand/writeLesson helpers defined, runTaskOnce executes verify after review succeeds, autopilot halts on verify fail with lesson appended. Minor: MAP.md line range for runTaskOnce was off by one (569-619 → 569-618), corrected during validation.
- **[2026-04-09] wiki-sources-frontmatter** — pass — Wiki system-guide files should list `sources:` in frontmatter as specific file:line ranges (not directories). Compilers may validate these paths; a directory breaks the contract. Corrected horizon-type-convention.md sources from directory path to proper function ranges during validation.
- **[2026-04-09] wiki-dual-sources-during-consolidation** — pass — T1 created new concepts/horizon-types.md (correct) but old systems/horizon-type-convention.md (stale) remained. Validator had to delete the stale file and update MAP.md references manually. When a new doc supersedes an existing one, executors should explicitly delete the old version in the same PR/commit; don't leave cleanup to the validator.
- **[2026-04-09] feynman-rl-audit** — pass — Reading every line of the RL loop (computeTickReward, scoreEndgameCandidates, writeScorecard, runTaskOnce verify path) revealed 5 bugs: npm test double-counted reward (+5 vs +3), type inference compared title words to slug prefixes (never matched), scorecards were never auto-written (function existed but nothing called it), verify failures never wrote lessons, and the parser rejected its own task IDs (T#1). All 5 fixed in one commit. Rule: after building any multi-component system, Feynman-test every connection by reading the code end-to-end before declaring it done.
- **[2026-04-10] unverified-resume-gap** — pass — The `[unverified]` filter was added to endgame and backlog paths but missed the in-progress "resume" path. When adding a skip-condition to a suggestion engine, grep every suggestion `kind` — each code path is independent and needs the same guard.
- **[2026-04-10] staleness-shell-injection** — pass — `checkStaleness` interpolated user-derived keywords into `execSync` shell strings (`grep "${kw}"`, `git log --grep="${kw}"`), allowing command injection via task titles containing `$`, backticks, or quotes. Fix: switch to `execFileSync` which bypasses the shell entirely. Rule: any function that runs shell commands with data derived from markdown content (task titles, inbox items) must use `execFileSync` with an args array, never string interpolation into `execSync`.

- **[2026-04-19] getContextFiles-bullets-verified** — pass — [resolved] Ran `test/experiments.test.js:252` "benchmark prompts render read files as bullet list" — passes. The 2026-04-08 `getContextFiles-dead-return` lesson was already fixed in code (only one `return` now, produces correct bullet format); the lesson just never got marked [resolved]. Self-heal loop caught the gap between shipped-fix and lesson-ledger.

- **[2026-04-19] healer-pre-ref-symbols** — pass — Extended `commands/clean.js:healBrokenMapRefs` to pre-scan MAP.md for Function Inventory pre-ref form `` `sym()` → `file:line` `` and cache (file:line) → symbol. When the main regex finds no post-ref context, the healer falls back to this cache. Verified by two new tests in `test/clean-heal.test.js`. Resolves 2026-04-08 `healer-needs-after-context`.

- **[2026-04-19] token-refresh-guards-wired** — pass — `commands/workflow.js` (plan/do/review), `commands/brainstorm.js`, `commands/integrations.js`, and `bin/atris.js` (agent/chat) all now call `ensureValidCredentials(apiRequestJson)` before using any token. Expired tokens trigger refresh; silent failures in agent mode and integrations are gone. Resolves 2026-02-10 `unguarded-token-usage`.

- **[2026-04-19] self-heal-seed-from-fail-lessons** — pass — New `pickUnresolvedFailLesson(cwd)` helper in `commands/autopilot.js` parses lessons.md, filters unresolved `fail` entries whose bug pattern is still present (via `isLessonResolved`), and seeds a priority-4.5 suggestion in `suggestNextTask`. The autopilot now proposes its own fixes from its own past failures — closing the see-it/ship-it loop. v3.6.0 core feature.

- **[2026-04-20] self-heal-ships-the-fix** — pass — v3.6.0 surfaced fail lessons; v3.7.0 plumbs the lesson content through autopilot context. `suggestNextTask` now attaches `lessonLine`/`lessonSlug`/`lessonDate` to self-heal suggestions; the autopilot loop passes them into `runTaskOnce` context; `buildPrompt` has dedicated plan + do branches that render the lesson and tell the planner to write a task with Exit/Verify/Rollback and the executor to commit as `fix: <slug>`. End-to-end: the system reads its own failure → plans the fix → ships the fix. Tests cover the plumbing without hitting `claude -p`.

- **[2026-04-20] version-bump-discipline** — pass — Bumping 3.5.0 → 4.20.0 for a feature release (self-healing loop) was wrong: the changes were additive, not breaking. Force-pushed a correction to v3.6.0 before npm publish. Rule: version bumps match change scope — additive = minor, breaking = major. "Big story" framing and cute dates are not reasons to jump majors.

- **[2026-04-20] typed-lessons-sidecar** — pass — Oracle design-review 2026-04-20 said prose + keyword grep cracks at 20-50 lessons. v3.8.0 adds `atris/lessons.json` as a typed sidecar (keyed by slug) carrying `detector`/`scope`/`applies_to`/`status`. `isLessonResolved` runs the detector (exit 0 = resolved); legacy prose lessons keep the keyword-grep path via `isLessonResolvedLegacy`. `pickUnresolvedFailLesson` skips `status: resolved` and `status: observed` (process rules, not fixable code states). 12 new tests. The schema is the compounding lever — prose stays human-readable, metadata stays machine-readable, neither side blocks the other.

- **[2026-04-20] dogfood-surfaces-real-bugs** — pass — Running `atris autopilot --dry-run` against its own repo after v3.7 surfaced two real bugs: (a) duplicate stale-wiki suggestion because `skipped.has(recompile:${pageName})` checked a different key than the skip branch added (`recompile:${suggestion.task}`); (b) T1 (unguarded-token-usage) still in backlog after v3.6 shipped the fix, because validator-cleanup pattern failed on merge. Fixed via uniform `suggestion.skipKey` field — callers push their canonical key, skip/dry-run branches add whatever is there. Rule: when multiple code paths write to a shared set under convention, put the key on the producer, not the consumer.

- **[2026-04-20] three-state-ledger** — pass — Oracle's second prescription shipped: `markLessonAttempted(cwd, slug, reason)` writes `status: attempted` + increments `attempts` + stamps `last_attempt` and `last_attempt_reason` to lessons.json when a self-heal tick fails at any of three paths (halt, review-rejected, verify-failed). `pickUnresolvedFailLesson` skips lessons with `status: attempted` AND `attempts >= 3` so stuck lessons surface to a human for re-scoping instead of looping the self-heal forever. Closes survivorship-bias: the ledger now records failures, not just successes.

- **[2026-04-20] staleness-parser-false-missing** — fail — [resolved] `checkPageStaleness` at `commands/clean.js:459` passed raw source strings like `` `bin/atris.js:199-340 (showHelp function)` `` to `fs.statSync`, which naturally failed → every source with a line-range or parenthesized annotation was reported as "(missing)" even when the file existed. This meant the stale check was giving false positives for years on any well-annotated source entry. Caught while dogfooding wiki-staleness-sweep endgame. Fix: strip trailing `(...)` and trailing `:N` / `:N-M` from the source string before stat. 5 new tests in `test/staleness-parser.test.js` cover happy path (strip + exists), missing-file detection preserved, and mtime-drift detection preserved.

- **[2026-04-20] dogfood-finds-process-wins** — pass — Using the atris process on atris itself (plan → do → review with real endgame + backlog) produced two wins beyond the documentation refresh: (a) exposed the staleness parser bug that false-flagged sources for months; (b) forced me to write file:line-annotated source refs in frontmatter, which is the format the repo-shape-manifest (v3.10) will standardize. Rule: when the system feels redundant, run it on itself — the friction surfaces real defects that normal coding skips past.

- **[2026-04-20] npm-test-default-breaks-non-node-repos** — fail — [resolved] `getVerifyCommand` in `commands/autopilot.js:775` returned `{cmd: null, explicit: false}` when a task had no explicit Verify field, AND the reactive-signal branch at `runTaskOnce` (comment: "Reactive signals use npm test as default") was firing `npm test` anyway. On swarlo (Python) this wrote a fresh `verify-fail-*` lesson every tick because `npm` wasn't even installed. Fix: `detectDefaultVerify(cwd)` checks `package.json` (with non-stub test script), then `pyproject.toml`/`pytest.ini`/`setup.py` → pytest, then Cargo.toml → cargo test, then go.mod → go test ./…, else null (skip verify). Verified: swarlo's getVerifyCommand now returns `pytest`. 10 new tests in `test/repo-shape.test.js`. Resolves oracle's third prescription (repo shape adapter).

- **[2026-04-20] cross-repo-dogfood-is-the-real-test** — pass — Running atris on itself converges to "all clean" fast (self-shaped code). Running atris on swarlo (Python) immediately exposed two real bugs (unhealable MAP refs on Python symbols, npm-test default) that atris-cli's own dogfood would never catch. Rule: a self-improvement system that only runs on itself compounds only the errors specific to itself. Portability tests are the real product test.
