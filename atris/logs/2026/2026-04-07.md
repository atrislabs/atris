# Log — 2026-04-07

## Handoff

**Context:** Shipped the local-first wiki slice and the deterministic upkeep layer. `atris/wiki/` is canonical, local is the default runtime, and `atris loop` now refreshes wiki health instead of leaving memory idle.
**Blockers:** none
**Next:** Decide whether cron-driven wiki upkeep and vibe-check interviewing should ship as separate follow-up slices.
**Learned:** The right sequencing was local wiki first, deterministic upkeep second, and only then scheduling or cloud automation.

---

## Completed ✅

- **C10:** Fix 1 broken reference in MAP.md [reviewed]

- **C9:** Fix broken MAP.md ref for `statusAtris()` → `commands/status.js:15-216` (T3) [reviewed]

- **C8:** Re-read sources and update atris/features/team-member-standard/build.md [reviewed]

- **C7:** Re-read sources and update atris/features/team-member-standard/build.md [reviewed]

- **C6:** Re-read sources and update atris/features/cli-ux-simplification/validate.md [reviewed]

- **C1:** Added a compact `atris/` tree to the README `Critical Files` section [✓ REVIEWED]
- **C2:** Shipped local-first wiki flow with canonical `atris/wiki/`, CLI aliases, sync aliasing, skill, and tests [✓ REVIEWED]
- **C3:** Wired wiki into `init`, `activate`, agent/spec docs, and seeded this repo's own `atris/wiki/` [✓ REVIEWED]
- **C4:** Shipped `atris loop` / `atris wiki loop` with stale/orphan detection, status/log refresh, skill docs, and smoke coverage [✓ REVIEWED]
- **C11:** Defined `Project Endstate` as a real feature pack with benchmark scope, build plan, and validation script [✓ REVIEWED]
- **C5:** Refreshed `atris/features/cli-ux-simplification/validate.md` last_compiled to 2026-04-07 [reviewed]

---

## In Progress 🔄

---

## Backlog

---

## Notes

- 00:00 PDT — Continuing README polish on the new day; adding a small `atris/` tree for faster onboarding.
- 00:01 PDT — Added the tree under `Critical Files` so the workspace shape is visible without scrolling through docs.
- 00:02 PDT — Claimed wiki execution work: local-first wiki flow, canonical `atris/wiki/`, cloud opt-in, tests, and feature pack updates.
- 00:03 PDT — Validation pass 1: `npm test -- test/commands.test.js` passed with the new wiki coverage.
- 00:04 PDT — Validation pass 2 initially exposed unrelated CLI plumbing gaps (`soul`, `fleet`, help text). Wired those commands into `bin/atris.js`, then reran the smoke suite clean.
- 00:05 PDT — Validation pass 3: full `npm test` passed, then manual smoke in a fresh temp directory confirmed `ingest`, `query`, and `lint` all target `atris/wiki/`.
- 00:06 PDT — Validation pass 4: reran full `npm test`; 94 tests passed again.
- 00:07 PDT — Validation pass 5: second fresh-temp manual smoke passed; `ingest` created `atris/wiki/`, then `query` and `lint` both resolved against the canonical local wiki root.
- 00:08 PDT — Starting wiki integration follow-up: scaffold on init, surface wiki state on activate, wire wiki into agent/spec docs, and dogfood `atris/wiki/` in this repo.
- 00:09 PDT — Seeded `atris/wiki/` for this repo with a workflow concept page and a repo overview synthesis.
- 00:10 PDT — Validation pass 6: `npm test -- test/cli-smoke.test.js` passed after adding wiki init/activate assertions.
- 00:11 PDT — Validation pass 7: full `npm test` passed again with wiki integration changes.
- 00:12 PDT — Validation pass 8: fresh workspace smoke passed for `init -> activate -> ingest -> query -> lint`.
- 00:13 PDT — Starting wiki upkeep loop feature: deterministic `atris loop` command, `/loop` skill, stale/orphan detection, next-ingest suggestions, and loop tests.
- 01:10 PDT — Wired the loop into README, MAP, and a dedicated `atris/features/wiki-loop/` pack so the upkeep slice is discoverable without replaying chat.
- 01:11 PDT — Validation pass 9: `npm test -- test/commands.test.js` passed with loop regression coverage.
- 01:12 PDT — Validation pass 10: `npm test -- test/cli-smoke.test.js` passed after adding an end-to-end `STATUS.md` + `log.md` refresh check for `atris loop`.
- 01:13 PDT — Validation pass 11: full `npm test` passed clean at 98/98.
- 01:13 PDT — Validation pass 12: fresh temp-dir smoke passed for `init -> loop -> wiki loop --json`; upkeep suggested `README.md` first and both wiki state files updated on disk.
- 23:29 PDT — Claimed the `Project Endstate` planning slice to turn the Mythos-vs-Avengers idea into a falsifiable benchmark instead of chat theater.
- 23:30 PDT — Review pass: created `atris/features/endstate/{idea,build,validate}.md`, registered the feature in `atris/features/README.md`, and kept the change docs-only so no test run was needed.
- 23:32 PDT — Narrowed `.gitignore` so `atris/features/endstate/` is trackable; otherwise the new feature pack would have stayed local-only and invisible to git.

---

## Inbox

- **I1: cron wrapper for `atris loop`.** Now that `atris loop` is the deterministic tick body, wrap it in a foreground daemon at `commands/wiki-autopilot.js`. Single-instance lock via `.atris/wiki.lock`. Default interval 30m, configurable via `--interval=Xm`. On each tick: run the same logic as `atris loop`. Background mode logs to `.atris/wiki-autopilot.log`. Graceful shutdown on SIGINT releases lock. The executor explicitly suggested this as the next move.
- **I2: capabilities.md generator.** New command `atris capabilities` (or auto-trigger from `atris activate` if missing/stale). Scans `bin/atris.js` else-if blocks → CLI commands; `commands/*.js` exports → handlers; `atris/skills/*/SKILL.md` frontmatter → skills. Writes `atris/wiki/concepts/capabilities.md` with autogen block delimiters so hand-edits survive. This is the "machine-readable list of primitives" the synthesis page (`atris/wiki/syntheses/atris-as-mini-agi.md`) identifies as the missing piece for intent→capability→composition.
- **I3: auto-ingest queue + hook.** Add `.atris/wiki-queue.md` (one path per line, deduped). After each `atris run`/`autopilot` cycle's REVIEW phase, append the modified files + journal Notes entries. New flag `atris ingest --drain-queue` reads the queue, prints one combined ingest prompt, atomically clears the queue. `atris activate` prints a soft nudge if queue is non-empty. The synthesis identifies this as the second blocking gap to mini-AGI.
- **I4: vibe-check skill (`wiki-vibe`).** Flow-style 5–7 question interview. Reads `wiki/STATUS.md` to seed. Walks the user through findings, surfaces contradictions, asks "what do you trust here". Writes session summary to `wiki/log.md`. Updates STATUS with `last vibe-check: YYYY-MM-DD`. Activate prints a soft nudge after 7 days. Empty wiki guard: skip if <5 pages.
- **I5: `atris briefing` command.** Reads STATUS.md + index.md + today's journal + TODO.md. Outputs a paste-ready daily digest (~500–1000 tokens) for system-prompt injection or terminal display. Sections: working on, wiki knows, stale, in flight, look at. Flags: `--save` writes to `atris/briefings/YYYY-MM-DD.md`, `--terse` prints under 100 tokens. Plain English, not log lines.
- **I6: multi-source / dirs / globs / URLs ingest.** Extend `wikiIngest` to accept arrays, recursive directory walks, glob expansion (manual walker, no new deps), and URL fetch (write to `os.tmpdir()`, ingest temp file). All resolved sources flow into the same prompt builder. Single-source case unchanged.
- **I7: chunking for sources >8K tokens.** New helper `lib/wiki-chunk.js` with `chunkSource(content, maxChars=32000)` that splits at paragraph boundaries. In `wikiIngest`, before printing the prompt, check size and chunk if needed. Print one prompt per chunk with `[chunk N/M of <filename>]` header. Pure character-based, no tokenizer dep.
- **I8: heal the orphan caught by `atris loop`.** Loop flagged `atris/wiki/syntheses/launch-post.md` as orphan (no inbound links from index.md). Quick fix: add launch-post to `wiki/index.md` under Syntheses. Validation: re-run `atris loop`, orphan count drops to 0.
