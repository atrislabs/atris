# Log — 2026-04-19

## Handoff

---

## Completed ✅

- **C1:** v3.6.0 — self-healing loop [reviewed]
  **Result:** Autopilot now reads its own `atris/lessons.md`, finds unresolved `fail` entries whose bug pattern is still present (per `isLessonResolved` grep), and seeds a priority-4.5 suggestion in `suggestNextTask`. Closes the see-it/ship-it loop.
- **C2:** Fix `getContextFiles-dead-return` [reviewed]
  **Result:** Verified already fixed in code (single `return` producing bullet format). Tagged the 2026-04-08 lesson `[resolved]`. `test/experiments.test.js:252` passes.
- **C3:** Fix `healer-needs-after-context` [reviewed]
  **Result:** Extended `commands/clean.js:healBrokenMapRefs` with a pre-pass that maps (file:line) → symbol from Function Inventory pre-ref form `` `sym()` → `file:line` ``. Two new tests in `test/clean-heal.test.js`.
- **C4:** Fix `unguarded-token-usage` (T1) [reviewed]
  **Result:** Wired `ensureValidCredentials(apiRequestJson)` into `commands/workflow.js` (plan/do/review), `commands/brainstorm.js`, `commands/integrations.js`, and `bin/atris.js` (agent/chat). Expired tokens now refresh instead of silently failing.
- **C5:** Clean fixture pollution from TODO.md and journal [reviewed]
  **Result:** Removed T12–T21 fixture tasks and journal fixture entries that were accumulating from test runs.
- **C6:** v3.7.0 — self-heal ships the fix [reviewed]
  **Result:** Extended v3.6 self-heal from surface-only to end-to-end. `suggestNextTask` attaches lesson metadata to self-heal suggestions; autopilot loop threads it through context; `buildPrompt` has dedicated plan + do branches that render the lesson content and steer the planner/executor to write a task with Exit/Verify/Rollback and commit as `fix: <slug>`. +4 prompt tests.
- **C7:** Version bump unwind (v4.20.0 → v3.6.0) [reviewed]
  **Result:** Caught post-push that jumping 3.5 → 4.20 for an additive feature violated semver. Force-pushed correction to v3.6.0, deleted remote v4.20.0 tag. New lesson `version-bump-discipline` logged.

---

## In Progress 🔄

---

## Backlog

---

## Notes

### v3.6.0 ship — 2026-04-19

Theme: self-healing loop. Instead of imagining new work via LLM, the autopilot now reads its own ledger of past failures and proposes fixes for the ones where grep confirms the bug is still present. This release shipped proof-of-work: three 2026-04-08 `fail` lessons that the horizon-imaginer had surfaced but no one had closed — all three resolved, ledger updated, new `pass` lessons appended. Tests: 200/200 pass (+6 new: 2 heal tests, 4 self-heal tests).

### v3.7.0 ship — 2026-04-20

Theme: self-heal ships the fix. v3.6 surfaced unresolved fails; v3.7 closes the loop by plumbing the lesson content through autopilot context into dedicated plan + do prompts. The planner now sees the full lesson (file:line refs included) and is told to write a task with Exit/Verify/Rollback; the executor is told to make the smallest change, run the verify command, and commit as `fix: <slug>`. Tests: 204/204 pass (+4 new prompt tests, no claude -p needed). The see-it → ship-it loop is now end-to-end.

### v3.8.0 ship — 2026-04-20

Theme: typed lessons. Oracle's design-review diagnosed the v3.6/v3.7 compounding ceiling: prose + fuzzy keyword grep cracks at 20-50 lessons. Fix is typed lessons with executable detectors. v3.8 ships `atris/lessons.json` as a sidecar keyed by slug, carrying `detector`/`scope`/`applies_to`/`status`. `isLessonResolved` runs the detector (exit 0 = resolved); the legacy keyword-grep path is preserved as `isLessonResolvedLegacy` for prose-only lessons. `pickUnresolvedFailLesson` now respects `status: resolved` (done) and `status: observed` (process rules, not fixable code states). Real sidecar ships with 4 entries — 2 resolved via detector, 2 observed. Dogfood also surfaced a skipped-key mismatch bug (duplicate stale-wiki suggestion) which got fixed in the same tick via `suggestion.skipKey`. Tests: 218/218 pass (+14 typed-lessons tests).

### v3.9.0 ship — 2026-04-20

Theme: three-state ledger — observed / attempted / resolved. Oracle's second prescription: "without `attempted`, the ledger only records fixes that worked — we keep fishing where we already caught fish." New `markLessonAttempted(cwd, slug, reason)` helper writes attempted status + increments attempts counter on sidecar when a self-heal tick fails. Wired into the three failure paths in `autopilotAtris`: halted, review-rejected, verify-failed. `pickUnresolvedFailLesson` caps self-heal retries at 3 attempts — lessons stuck above the cap get surfaced to humans for re-scoping. Tests: 225/225 pass (+7 attempted-ledger tests).

### v3.9.1 ship — 2026-04-20

Theme: wiki-staleness-sweep + dogfood-found parser fix. Used the Atris process (endgame + backlog T40–T47 + plan/do/review discipline) on atris-cli itself to refresh 8 drifted wiki pages against their sources. Mid-sweep, dogfooding exposed a real defect in `checkPageStaleness`: source strings with line ranges or parenthesized annotations (the standard format) were passed raw to `fs.statSync` and reported as "(missing)" even when the file existed — silently false-positive for months. Fix strips `(annotation)` and `:line-range` suffixes before stat. After both fixes, `atris clean --dry-run` now reports: 0 stale tasks, 0 unhealable MAP refs, 0 stale wiki pages. Tests: 230/230 pass (+5 staleness-parser tests).

---

- 11:00 pm
  I stopped for a manual check.
  We are still on the agent-commands-solid endgame.
  Next tick will look for new work.


## Inbox


