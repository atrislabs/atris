#!/bin/sh
# House rule: no Co-Authored-By Claude trailers in this repo's commits.
if grep -qi "co-authored-by: claude" "$1"; then
  grep -vi "co-authored-by: claude" "$1" > "$1.tmp" && mv "$1.tmp" "$1"
fi
exit 0
